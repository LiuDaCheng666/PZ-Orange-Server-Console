# 更新记录

## 0.8.0

- 物品发放改为逐目标跟踪游戏日志结果，区分排队、送达、游戏确认成功、明确失败和待确认。
- 批量发放增加客户端唯一提交 ID；网络重试、页面刷新或重复点击会恢复原任务，不会再次入队。
- 发放弹窗展示实时进度，页面可通过提交 ID 恢复执行状态，避免命令已经生效却长期停在“等待面板接收”。
- 执行历史补充物品发放确认统计和待确认警告，未捕获明确日志结果时提醒管理员先核对再重试。
- GitHub 公开构建加入实机桌面、手机、AI 审查、物品库、弹窗和物理机控制截图。

## 0.7.0

- 系统监控页新增物理机控制：Web 保留管理员 `admin` 可在全部游戏服务器停止后安排 30 秒 Windows 重启，并可在倒计时内取消。
- 新增 Web 开机启动计划任务，使用当前 Windows 管理员身份、S4U 登录类型和最高权限，在进入桌面前恢复面板。
- 自动进入桌面通过本机微软 Sysinternals Autologon 工具配置，Windows 密码不经过 Web 面板。
- 主机控制接口继续使用登录会话、CSRF 校验、固定确认文字和审计日志；普通 Web 用户不能执行物理机操作。

## 0.6.3

- AI 只读配置检索支持“一天多少小时、一天多长、昼夜、现实多久”等中文自然问法，并稳定关联 `SandboxVars.DayLength`。
- `DayLength` 证据会附带当前枚举值的实际时长说明，例如 `4` 表示现实 1 小时 30 分钟对应游戏内一天。
- 睡眠、睡觉、入睡、疲劳等问题会关联服务器 ini 中的 `SleepAllowed`、`SleepNeeded` 等只读设置。
- 增加中文日长和睡眠配置回归测试，防止知识库泛词把检索结果带偏到 `Mods=`。

## 0.6.2

- 修复 AI 将面板配置 ID（如 `production`）与 PZ 内部实例名（如 `servertest`）误判为不同服务器的问题。
- 只读配置证据现在明确标记当前选中服务器、面板配置 ID、PZ 内部实例名和权威配置类型。
- 系统规则明确要求按当前 Profile 的 `dataRoot + serverName` 识别权威 SandboxVars/ini，不得仅凭文件名前缀否定证据。

## 0.6.1

- 面板品牌调整为“PZ 橙子服务器控制台”，主视觉由绿色改为橙色。
- Mod 更新计划增加“发现更新后自动安全重启”。
- 自动重启使用固定 60 秒通知，联动原生广播和 PZWebNotices 弹窗。
- 执行历史限制为每页 30 条，并增加分页和分类筛选。
- Web 内置 AI Bridge 增加服务器信息库与本地文件夹快速打开入口。
- AI 查询本服设置时优先使用只读配置和实时遥测，证据不足时拒绝猜测。
- AI 只读配置检索增加税率、成交税、上架费、维护费、车辆税和奖池税等中文语义映射。
- 知识库资料中出现自定义 SandboxVars 字段名时，Bridge 会回查当前服务器配置并返回实际值，不再只引用教程默认值。
- 配置证据过滤扩大到任何密码、RCON、Token、Secret、API Key 和凭据类字段。
- SandboxVars 读取保留嵌套 Mod 的完整字段路径，例如 `AirdropMod.Enabled`，并过滤英文问句停用词，减少跨 Mod 误匹配。
- 服务器信息库新增 `知识库填写要求.md`，统一中文同义词、完整字段路径、单位、取值和当前值回查规则。
- 知识库中的 `SandboxVars.ModName.FieldName` 标准路径会自动归一化并回查当前选中服务器的实际配置。
- 普通玩家保持只读沙盒，AI 不获得 Shell、Lua、RCON 或管理命令权限。
- 增加有限会话、频率控制、越权绕过审查和单独审查名单。
- 审查处罚支持踢出玩家、全服广播、弹窗提醒和相关会话保留。
- AI Bridge 可为未携带 SteamID 的旧版 Agent 请求按服务器玩家目录补全身份，避免正常玩家被误拒绝。
- 公开发行包改为首次从服务器本机创建 Web 管理员，不携带固定密码。
- 公开构建增加白名单打包、空白知识库、敏感信息扫描、ZIP 和 SHA256。
