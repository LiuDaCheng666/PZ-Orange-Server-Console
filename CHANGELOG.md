# 更新记录

## 0.9.0

- 修复“撤销权限”发送当前 PZ 版本不支持的 `none` 等级而实际未撤权的问题；现在统一降级为普通玩家 `user`，兼容旧页面请求，并将服务器返回的未知权限等级或玩家不存在识别为失败而不是成功。
- 总览第一项改为“全服在线合计”，汇总所有运行中服务器的在线连接数，同时保留当前所选服务器人数、上限和运行服数量；某个运行服人数尚未同步时显示已知人数下限并明确标注待同步服务器数量。
- 修复首次启动2服时 GC 日志父目录不存在导致 JVM 在游戏代码运行前以退出码 `1` 结束；托管器会预创建数据和日志目录，非零退出记录为失败，启动接口等待 Java 稳定运行并把快速退出直接返回页面。
- 修复 JVM 遥测把 `D:/...` Windows 绝对路径的盘符冒号误判为 `-Xlog` 分隔符，导致使用独立 GC 日志路径的2服不显示 JVM 堆数据；同时覆盖相对路径、反斜杠和带空格引号路径。
- Web 托管启动、手动安全重启和 Mod 更新自动重启产生的新游戏 Java 进程，默认使用 Windows“高于正常（AboveNormal）”优先级；启动状态会记录并校验实际优先级。
- 本机资源监控新增逐逻辑处理器占用网格，按当前所选服务器 Java 进程的 CPU 亲和性高亮允许调度的核心；每个逻辑核直接标注所有允许在该核调度的服务器名称和 PID 图例，并明确区分主机逐核负载与单进程占用。
- 修复本机监控读取 Windows 开机计划任务状态时，将 `UInt32.MaxValue` 占位结果 `4294967295` 强制转换为 `Int32` 导致整个 `/api/system` 请求失败；未定义结果现在显示为空，真实任务结果使用 `Int64` 保留。
- 修复物品已由游戏服务器确认、执行历史也已记录成功，但前端并发查询被串行 HTTP 日志扫描阻塞时仍停在“等待返回发放结果”的竞态；页面现在先查询轻量持久化终态，再查询逐命令日志，单次日志或弹窗回执失败会继续重试而不是丢弃已完成结果。
- 物品发放 POST 现在会在入队后短暂等待游戏日志终态，并把成功或失败随原始响应直接返回；单人任务同时兼容旧页面的通用命令回执路径，即使浏览器标签页仍运行旧脚本也不再依赖后续批量轮询才能结束等待。
- 玩家管理新增 SteamID64 档案查询，可查看同一 SteamID 关联的白名单账号、角色存档、在线状态、权限、最近登录、允许列表和封禁状态。
- Web 保留管理员 `admin` 可在本机“Web 用户”页给指定账号授予玩家档案管理权限；授权账号可查询 SteamID 档案、修改关联账号密码和执行停服删除，修改密码前仍会校验账号归属并通过 PZ 官方 `setpassword` 命令执行。
- 玩家档案授权不会同时授予物理机控制或 Web 用户管理权限；权限开关只能由本机登录的保留管理员 `admin` 修改，历史旧账号默认不授权。
- 玩家密码不会进入 Web 命令跟踪、执行历史或 API 回包；通用白名单账号命令也统一增加管理员权限和密码脱敏。
- 支持永久删除指定 SteamID 的账号、角色和允许列表数据。删除只允许在服务器完全停止且无生命周期任务时执行，并先备份账号与角色 SQLite 数据库。
- 删除玩家数据时保留 SteamID 封禁记录与 Web 审计日志，避免删除账号意外解封或丢失管理追踪。
- AI 助手页新增“构建服务器知识库”，可选择服务器和临时构建模型。
- 构建流程先在本机只读解析服务器 ini、SandboxVars、启用 Mod、Workshop 项目和本地 `mod.info`，通过脱敏与字段覆盖检查后才调用 Provider。
- 脱敏资料自动分块，知识库构建可独立选择 Auto、Low、Medium 或 High；Auto 对 DeepSeek/Flash 类模型使用 Low、其他 Responses 模型使用 Medium，Chat 兼容接口由模型自身控制推理。
- DeepSeek Flash 知识库构建限制为 Auto/Low；官方 `api.deepseek.com` 的 V4 Pro 构建会自动从尚未开放的 Responses 路由切换到已验证可用的 Chat Completions 路由，并传递显式 Low/Medium/High。其他兼容 Provider 命中特定“请使用 Flash”错误时仍可自动降级续跑。
- 模型输出中的 `SandboxVars.BuildingCraft*` 等通配提示，以及 `SandboxVars.lgd_antibodies_194_` 等能够匹配多个真实字段的无星号字段族前缀，会移除 `SandboxVars.` 后作为普通检索提示保留；不存在的完整 SandboxVars 路径仍会阻止发布。
- 模型增强校验改用本次构建全部 SandboxVars 字段，不再因跨分段引用真实配置组而误判；少量孤立无效引用只删除对应 Markdown 行，过多或高占比异常仍拒绝发布。
- Provider 或校验失败时保留已完成批次；使用相同服务器、模型和强度再次构建会从失败批次断点续建，管理员主动取消或更换配置时清理暂存目录。
- Responses 高推理构建预留 16000 个输出 token，并识别 DeepSeek 嵌套正文、仅返回推理过程和输出 token 耗尽状态，避免笼统显示“空内容”。
- 物品发放弹窗增加唯一提交 ID 的持久化执行历史回查；Web 重启或内存回执丢失后，只要后台已记录完成，页面就会结束等待并显示最终结果。
- 物品发放结果改为从命令提交游标按 4 MB 分段连续扫描，不再在日志增长超过 256 KB 时跳到文件尾；跨段命令状态会保留，成功或失败终态会缓存，避免玩家已收到物品但面板持续等待。
- 物品发放提交响应超时由 5 秒调整为 15 秒，降低批量发放已经入队但前端仍停在“等待面板接收命令”的误判。
- 模型输出增加凭据、SteamID、本机绝对路径和虚构 SandboxVars 路径二次校验，失败不会替换当前知识库。
- 新知识库只在全部批次成功后发布；取消、接口失败或面板中断时保留现有自动知识库和外部手工资料。
- 自动生成知识库与服主放入 `服务器信息库` 的外部资料继续共同参与玩家问答检索，当前值仍以实时只读配置为最高证据。

## 0.8.0

- 物品发放改为逐目标跟踪游戏日志结果，区分排队、送达、游戏确认成功、明确失败和待确认。
- 批量发放增加客户端唯一提交 ID；网络重试、页面刷新或重复点击会恢复原任务，不会再次入队。
- 发放弹窗展示实时进度，页面可通过提交 ID 恢复执行状态，避免命令已经生效却长期停在“等待面板接收”。
- 执行历史补充物品发放确认统计和待确认警告，未捕获明确日志结果时提醒管理员先核对再重试。
- GitHub 公开构建加入实机桌面、手机、AI 审查、物品库、弹窗和物理机控制截图。

## 0.7.0

- 系统监控页新增物理机控制：Web 保留管理员 `admin` 可在全部游戏服务器停止后安排 30 秒 Windows 重启，并可在倒计时内取消。
- 新增 Web 开机启动计划任务，使用当前 Windows 管理员身份、S4U 登录类型和最高权限，在进入桌面前恢复面板。
- 自动进入桌面通过本机微软 Sysinternals Autologon 工具配置，Windows 密码不经过 Web 面板。
- 主机控制接口继续使用登录会话、CSRF 校验、固定确认文字和审计日志；普通 Web 用户不能执行物理机操作。

## 0.6.3

- AI 只读配置检索支持“一天多少小时、一天多长、昼夜、现实多久”等中文自然问法，并稳定关联 `SandboxVars.DayLength`。
- `DayLength` 证据会附带当前枚举值的实际时长说明，例如 `4` 表示现实 1 小时 30 分钟对应游戏内一天。
- 睡眠、睡觉、入睡、疲劳等问题会关联服务器 ini 中的 `SleepAllowed`、`SleepNeeded` 等只读设置。
- 增加中文日长和睡眠配置回归测试，防止知识库泛词把检索结果带偏到 `Mods=`。

## 0.6.2

- 修复 AI 将面板配置 ID（如 `production`）与 PZ 内部实例名（如 `servertest`）误判为不同服务器的问题。
- 只读配置证据现在明确标记当前选中服务器、面板配置 ID、PZ 内部实例名和权威配置类型。
- 系统规则明确要求按当前 Profile 的 `dataRoot + serverName` 识别权威 SandboxVars/ini，不得仅凭文件名前缀否定证据。

## 0.6.1

- 面板品牌调整为“PZ 橙子服务器控制台”，主视觉由绿色改为橙色。
- Mod 更新计划增加“发现更新后自动安全重启”。
- 自动重启使用固定 60 秒通知，联动原生广播和 PZWebNotices 弹窗。
- 执行历史限制为每页 30 条，并增加分页和分类筛选。
- Web 内置 AI Bridge 增加服务器信息库与本地文件夹快速打开入口。
- AI 查询本服设置时优先使用只读配置和实时遥测，证据不足时拒绝猜测。
- AI 只读配置检索增加税率、成交税、上架费、维护费、车辆税和奖池税等中文语义映射。
- 知识库资料中出现自定义 SandboxVars 字段名时，Bridge 会回查当前服务器配置并返回实际值，不再只引用教程默认值。
- 配置证据过滤扩大到任何密码、RCON、Token、Secret、API Key 和凭据类字段。
- SandboxVars 读取保留嵌套 Mod 的完整字段路径，例如 `AirdropMod.Enabled`，并过滤英文问句停用词，减少跨 Mod 误匹配。
- 服务器信息库新增 `知识库填写要求.md`，统一中文同义词、完整字段路径、单位、取值和当前值回查规则。
- 知识库中的 `SandboxVars.ModName.FieldName` 标准路径会自动归一化并回查当前选中服务器的实际配置。
- 普通玩家保持只读沙盒，AI 不获得 Shell、Lua、RCON 或管理命令权限。
- 增加有限会话、频率控制、越权绕过审查和单独审查名单。
- 审查处罚支持踢出玩家、全服广播、弹窗提醒和相关会话保留。
- AI Bridge 可为未携带 SteamID 的旧版 Agent 请求按服务器玩家目录补全身份，避免正常玩家被误拒绝。
- 公开发行包改为首次从服务器本机创建 Web 管理员，不携带固定密码。
- 公开构建增加白名单打包、空白知识库、敏感信息扫描、ZIP 和 SHA256。
