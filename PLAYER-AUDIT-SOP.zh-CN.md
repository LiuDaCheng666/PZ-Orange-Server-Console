# PZ 玩家反作弊深度审计 SOP

## 目的与边界

本流程用于管理员对选中玩家进行只读调查。证据来自当前服务器 Profile 对应的日志和 `global_mod_data.bin` 经济快照。AI 只负责归纳、交叉核验和给出建议，不得自动踢出、封禁、删档、改密码或执行任何服务器命令。

“没有日志命中”只能写成“在当前时间范围和现有日志中未发现”，不能写成“证明没有作弊”。

## 标准流程

1. **确认身份**：用 SteamID64 为主键，核对历史用户名、脱敏 IP、连接记录、角色权限。用户名可能变化，不能只按显示名定人。
2. **核对权限**：检查 admin 日志和连接角色。管理员、GM、观察者产生的管理行为必须和其授权时间对应。
3. **检查原生反作弊**：检查速度、Lua 校验和其他原生信号。所有 `Speed` 记录都只是弱线索；带 `cooldown`、缺少 `speed` 数值、`speed` 小于 35，或已标记 `likelyNetworkNoise` / `evidenceWeight=noise` 的记录一律计为网络或协议噪声，证据权重为零。日志里的 `action=Kick` 只说明原版反作弊执行了动作，不会把噪声升级为作弊证据。其余带明确数值的速度记录也必须结合连接质量、载具状态和独立的服务端权威证据，不能单独定性。
4. **检查高危命令与物品替换**：优先检查服务端已拦截命令、调试命令、跨玩家目标命令和 `blocked_item_transform`。非法物品替换记录由服务端在 `ItemTransaction` 执行前生成，能证明客户端提交了不在载体允许列表中的目标类型且请求已被拒绝；它不能证明目标物品已经生成。
5. **检查物品与管理日志**：核对 item/admin/map/PVP 日志。重点找直接生成、复制、删除、传送、改权限及异常批量操作。
6. **检查 Mod 命令**：统计命令总量和峰值。请求次数不代表成功次数；必须结合服务端回执、物品删除结果或权威状态变化。
7. **核对经济总账**：以服务端 `flowEvents` 为权威，按事件顺序验证 `balanceAfter` 连续性。余额无来源增长比单纯高余额更重要。
8. **排除独立钱包**：`safehouse_water_refill` 等独立钱包支出不应扣个人余额。新增 Mod 钱包时必须更新读取器白名单后再判断跳账。
9. **核对转账双方**：转账要验证双方同一时间、同一金额、方向相反。只有单边记录时标记为需要人工复核。
10. **核对回收**：确认服务端真实删除物品，并比较单次金额、总额和全服排名。金额高但位于正常全服分布内，不能直接判作弊。
11. **核对悬赏与交易**：检查提交者、发布者、托管金、支付状态和物品实例快照。真实快照与完整托管链可降低风险。
12. **区分管理员审计**：只有服务端连接日志的角色权限或服务端可信 `adminPower=true` 才能证明管理员身份。已授权管理操作保留审计但计 0 分；用户名叫 `admin` 不能获得豁免，Java Agent 实际阻断和独立反作弊异常仍要展示。
12. **核对 Lifestyle 接口**：`LS.AddItemToPlayer` 是潜在通用入口。同毫秒、同位置的 Add/Remove 配对通常是消耗品正常流程；未配对 Add 需要重点复核。
13. **给出结论**：分别列出支持怀疑和降低怀疑的证据，说明日志窗口及缺失项，再给出分级和下一步人工动作。

## 速度信号硬性规则

- 同一玩家重复出现的 `Speed` 或 `Speed + cooldown` 不是多个独立证据，不能按条数累加成高风险。
- 速度类发现的严重程度最高只能是 `warning`。
- 如果全部可疑证据都来自速度日志，总结论最高只能是“需要观察”，不得输出“高度可疑”或“证据确凿”。
- 只有与速度无关的独立服务端证据，例如直接生成/复制、余额无来源增长、未授权管理命令或可靠物品快照，才能支持更高结论。

## 结论分级

- **未发现**：当前证据没有可靠异常，或可疑信号均被权威流水/成对操作合理解释。
- **需要观察**：存在单一弱信号、证据缺失或无法完成双边核验，建议扩大时间范围或保留观察。
- **高度可疑**：多个独立证据相互支持，但仍缺少直接服务端生成/复制或无来源增长证据。
- **证据确凿**：存在服务端直接生成/复制、余额无来源增长、明确管理命令滥用、可靠物品快照或同等级直接证据。

## AI 输出要求

AI 必须输出结构化 JSON，包含结论、风险等级、可信度、摘要、发现、局限和建议。不得输出任何可执行命令，不得要求或显示 API Key、密码和本机绝对路径，不得把 IP 等身份信息扩散到审计以外。

最终处罚必须由有玩家档案管理权限的 Web 管理员人工确认。AI 报告与“封禁 SteamID”按钮保持完全分离。

## 人工复核清单

- SteamID 与用户名是否正确关联
- 查询的是正确服务器和正确时间范围
- 日志是否发生轮换或缺失
- 原生反作弊是否可能由网络、载具或版本差异触发
- 余额连续性是否排除了独立钱包
- 转账是否完成双边核验
- 回收是否有物品删除和全服分布支持
- 悬赏是否有物品快照、提交者和托管支付链
- AddItem 是否存在同时间 Remove 配对
- 是否存在可复现的直接作弊证据
- 是否明确记录了不确定性
- 是否由管理员人工决定处罚
